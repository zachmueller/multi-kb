run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gosimple
    - gofmt
    - goimports

linters-settings:
  goimports:
    local-prefixes: github.com/zmueller/multi-kb
