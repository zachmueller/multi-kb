# CLI

Go binary providing the `multi-kb` command. Operates in client mode (local) or server mode (EC2) determined by configuration. See the [CLI spec](../specs/01-mvp/cli/spec.md) for full requirements.
