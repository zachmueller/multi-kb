import { Construct } from "constructs";

export interface SubmitLambdaProps {
  // Properties will be defined in SUB-001.
}

export class SubmitLambda extends Construct {
  constructor(scope: Construct, id: string, _props: SubmitLambdaProps) {
    super(scope, id);
    // Implementation in SUB-001.
  }
}
