import { Construct } from "constructs";

export interface RecallLambdaProps {
  // Properties will be defined in RCL-001.
}

export class RecallLambda extends Construct {
  constructor(scope: Construct, id: string, _props: RecallLambdaProps) {
    super(scope, id);
    // Implementation in RCL-001.
  }
}
