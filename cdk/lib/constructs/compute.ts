import { Construct } from "constructs";

export interface ComputeProps {
  // Properties will be defined in CMP-001.
}

export class Compute extends Construct {
  constructor(scope: Construct, id: string, _props: ComputeProps) {
    super(scope, id);
    // Implementation in CMP-001.
  }
}
