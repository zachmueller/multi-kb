import { Construct } from "constructs";

export interface ObservabilityProps {
  // Properties will be defined in OBS-001.
}

export class Observability extends Construct {
  constructor(scope: Construct, id: string, _props: ObservabilityProps) {
    super(scope, id);
    // Implementation in OBS-001.
  }
}
