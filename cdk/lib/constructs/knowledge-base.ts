import { Construct } from "constructs";

export interface KnowledgeBaseProps {
  // Properties will be defined in KB-001.
}

export class KnowledgeBase extends Construct {
  constructor(scope: Construct, id: string, _props: KnowledgeBaseProps) {
    super(scope, id);
    // Implementation in KB-001.
  }
}
