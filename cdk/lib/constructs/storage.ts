import { Construct } from "constructs";

export interface StorageProps {
  // Properties will be defined in STR-001.
}

export class Storage extends Construct {
  constructor(scope: Construct, id: string, _props: StorageProps) {
    super(scope, id);
    // Implementation in STR-001.
  }
}
